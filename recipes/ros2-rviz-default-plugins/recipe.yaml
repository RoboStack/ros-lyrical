# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-rviz-default-plugins
  version: 15.2.5
source:
  git: https://github.com/ros2-gbp/rviz-release.git
  tag: release/lyrical/rviz_default_plugins/15.2.5-1
  target_directory: ros2-rviz-default-plugins/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 25
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/ros2/rviz/blob/ros2/README.md
  repository: https://github.com/ros2/rviz
  license: BSD-3-Clause
  summary: Several default plugins for rviz to cover the basic functionality.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - qt6-main
    - ros2-ament-cmake-gmock
    - ros2-ament-cmake-gtest
    - ros2-ament-cmake-lint-cmake
    - ros2-ament-cmake-ros
    - ros2-ament-index-cpp
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-geometry-msgs
    - ros2-gz-math-vendor
    - ros2-image-transport
    - ros2-interactive-markers
    - ros2-laser-geometry
    - ros2-map-msgs
    - ros2-nav-msgs
    - ros2-pluginlib
    - ros2-point-cloud-transport
    - ros2-rclcpp
    - ros2-resource-retriever
    - ros2-resource-retriever-service-plugin
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rviz-common
    - ros2-rviz-ogre-vendor
    - ros2-rviz-rendering
    - ros2-rviz-rendering-tests
    - ros2-rviz-visual-testing-framework
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-urdf
    - ros2-visualization-msgs
  run:
    - python
    - qt6-main
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-geometry-msgs
    - ros2-gz-math-vendor
    - ros2-image-transport
    - ros2-interactive-markers
    - ros2-laser-geometry
    - ros2-map-msgs
    - ros2-nav-msgs
    - ros2-pluginlib
    - ros2-point-cloud-transport
    - ros2-rclcpp
    - ros2-resource-retriever
    - ros2-resource-retriever-service-plugin
    - ros2-ros-workspace
    - ros2-rviz-common
    - ros2-rviz-ogre-vendor
    - ros2-rviz-rendering
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-urdf
    - ros2-visualization-msgs
