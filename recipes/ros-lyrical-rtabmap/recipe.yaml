# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-rtabmap
  version: 0.22.1
source:
  git: https://github.com/ros2-gbp/rtabmap-release.git
  tag: release/lyrical/rtabmap/0.22.1-3
  target_directory: ros-lyrical-rtabmap/src/work
  patches:
    - patch/ros-lyrical-rtabmap.patch

build:
  script: ${{ '$RECIPE_DIR/build_catkin.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_catkin.bat' }}
  number: 22
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://introlab.github.io/rtabmap
  repository: https://github.com/introlab/rtabmap
  license: BSD-3-Clause
  summary: RTAB-Map's standalone library. RTAB-Map is a RGB-D SLAM approach with real-time constraints.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - ${{ 'libdc1394' if not win }}
    - ${{ 'libgl-devel' if linux }}
    - ${{ 'libopengl-devel' if linux }}
    - if: build_platform == target_platform
      then:
        - pkg-config
    - ceres-solver
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - libusb
    - numpy
    - pcl
    - pip
    - proj
    - python
    - ros-lyrical-cv-bridge
    - ros-lyrical-gtsam
    - ros-lyrical-libg2o
    - ros-lyrical-qt-gui-cpp
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.18.* lyrical_*
    - sqlite 3.*
    - vtk
    - vtk-base
    - zlib
  run:
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - pcl
    - python
    - ros-lyrical-cv-bridge
    - ros-lyrical-gtsam
    - ros-lyrical-libg2o
    - ros-lyrical-qt-gui-cpp
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.18.* lyrical_*
    - sqlite
    - vtk-base
    - zlib
