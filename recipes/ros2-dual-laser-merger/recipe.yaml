# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-dual-laser-merger
  version: 0.0.1
source:
  git: https://github.com/ros2-gbp/dual_laser_merger-release.git
  tag: release/lyrical/dual_laser_merger/0.0.1-3
  target_directory: ros2-dual-laser-merger/src/work
  patches:
    - patch/ros-lyrical-dual-laser-merger.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 25
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  repository: https://github.com/pradyum/dual_laser_merger
  license: Apache-2.0
  summary: merge dual lidar's scans.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - numpy
    - pcl
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-auto
    - ros2-ament-copyright
    - ros2-ament-cpplint
    - ros2-ament-flake8
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-ament-pep257
    - ros2-ament-xmllint
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-geometry-msgs
    - ros2-laser-geometry
    - ros2-message-filters
    - ros2-pcl-conversions
    - ros2-pcl-ros
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-tf2
    - ros2-tf2-ros
    - ros2-tf2-sensor-msgs
    - vtk-base
  run:
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - pcl
    - python
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-geometry-msgs
    - ros2-laser-geometry
    - ros2-message-filters
    - ros2-pcl-conversions
    - ros2-pcl-ros
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-ros-workspace
    - ros2-tf2
    - ros2-tf2-ros
    - ros2-tf2-sensor-msgs
    - vtk-base
