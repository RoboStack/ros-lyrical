# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-moveit-planners-ompl
  version: 2.15.1
source:
  git: https://github.com/ros2-gbp/moveit2-release.git
  tag: release/lyrical/moveit_planners_ompl/2.15.1-1
  target_directory: ros2-moveit-planners-ompl/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 25
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://moveit.ai
  repository: https://github.com/ros-planning/moveit2
  license: BSD-3-Clause
  summary: MoveIt interface to OMPL
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gtest
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-eigen3-cmake-module
    - ros2-moveit-common
    - ros2-moveit-core
    - ros2-moveit-msgs
    - ros2-moveit-resources-fanuc-moveit-config
    - ros2-moveit-resources-panda-moveit-config
    - ros2-moveit-resources-pr2-description
    - ros2-moveit-ros-planning
    - ros2-ompl
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-tf2-eigen
    - ros2-tf2-ros
  run:
    - python
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-moveit-common
    - ros2-moveit-core
    - ros2-moveit-msgs
    - ros2-moveit-ros-planning
    - ros2-ompl
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-ros-workspace
    - ros2-tf2-eigen
    - ros2-tf2-ros
