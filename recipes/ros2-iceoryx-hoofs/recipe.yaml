# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-iceoryx-hoofs
  version: 2.95.8

build:
  number: 25
  script: ''
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://iceoryx.io
  repository: https://github.com/eclipse-iceoryx/iceoryx
  license: Apache-2.0
  summary: Eclipse iceoryx inter-process-communication (IPC) middleware basic building blocks
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build: []
  host:
    - libacl
    - python
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-ros-environment
    - ros2-ros-workspace
  run:
    - libacl
    - libiceoryx-hoofs >=2.95.8, <2.95.9a0
    - python
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-ros-workspace
