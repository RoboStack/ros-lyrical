# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-autoware-lanelet2-utils
  version: 1.7.0
source:
  git: https://github.com/ros2-gbp/autoware_core-release.git
  tag: release/humble/autoware_lanelet2_utils/1.7.0-1
  target_directory: ros2-autoware-lanelet2-utils/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 25
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: The autoware_lanelet2_utils package
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - range-v3
    - ros2-ament-cmake-auto
    - ros2-ament-cmake-ros
    - ros2-ament-index-cpp
    - ros2-autoware-cmake
    - ros2-autoware-lanelet2-extension
    - ros2-autoware-map-msgs
    - ros2-autoware-planning-msgs
    - ros2-autoware-pyplot
    - ros2-autoware-test-utils
    - ros2-autoware-utils-geometry
    - ros2-autoware-utils-math
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-rclcpp
    - ros2-rclpy-message-converter
    - ros2-ros-environment
    - ros2-ros-workspace
    - yaml-cpp
  run:
    - python
    - range-v3
    - ros2-autoware-lanelet2-extension
    - ros2-autoware-map-msgs
    - ros2-autoware-planning-msgs
    - ros2-autoware-utils-geometry
    - ros2-autoware-utils-math
    - ros2-distro-mutex 0.20.* lyrical_*
    - ros2-rclcpp
    - ros2-ros-workspace
